//List of Enums

enum TextSizes { small, medium, large }

enum OrderStatus { processing, shipped, delivered }

enum PaymentMethods {
  paypal,
  googlePlay,
  applePlay,
  visa,
  masterCard,
  creditCard,
  gCash,
  payMaya,
}
